# Trustless Work API key (required for deploy/fund/release).
# Get it from https://dapp.trustlesswork.com → Settings → API Keys → Testnet → Request API Key.
# Copy the key once; it cannot be viewed again.
NEXT_PUBLIC_API_KEY=

# Use "development" (testnet) for local dev, "mainnet" for production.
# Must match the network of your API key (Testnet key → development).
NEXT_PUBLIC_TW_NETWORK=development
